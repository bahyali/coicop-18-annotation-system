═══════════════════════════════════════════════════════════════
  أوامر سريعة لتشغيل COICOP Classification System
═══════════════════════════════════════════════════════════════

📦 الصور على Docker Hub:
   - radhyah/coicop-backend:latest
   - radhyah/coicop-frontend:latest

═══════════════════════════════════════════════════════════════
🚀 تشغيل سريع على جهاز جديد
═══════════════════════════════════════════════════════════════

1️⃣ تسجيل الدخول:
   docker login -u radhyah

   Password/Token: YOUR_DOCKER_TOKEN_HERE

2️⃣ تشغيل المشروع:
   docker-compose -f docker-compose.production.yml up -d

3️⃣ الوصول للتطبيق:
   الواجهة: http://localhost:8080
   API: http://localhost:8000
   الوثائق: http://localhost:8000/docs

═══════════════════════════════════════════════════════════════
🔧 أوامر إدارة
═══════════════════════════════════════════════════════════════

⏸️  إيقاف:
   docker-compose -f docker-compose.production.yml down

📋 عرض السجلات:
   docker-compose -f docker-compose.production.yml logs -f

🔄 إعادة تشغيل:
   docker-compose -f docker-compose.production.yml restart

🆕 تحديث الصور:
   docker-compose -f docker-compose.production.yml pull
   docker-compose -f docker-compose.production.yml up -d

═══════════════════════════════════════════════════════════════
📊 معلومات إضافية
═══════════════════════════════════════════════════════════════

✅ الصور: خاصة (Private) - تحتاج تسجيل دخول
✅ البيانات: محفوظة في database.db
✅ المنافذ: 8080 (Frontend), 8000 (Backend)

═══════════════════════════════════════════════════════════════

⚠️  ملاحظة مهمة:
   لجعل الـ Repositories خاصة يدوياً:
   1. اذهب إلى: https://hub.docker.com/repositories/radhyah
   2. اضغط على كل repository
   3. Settings → Make Private

═══════════════════════════════════════════════════════════════
