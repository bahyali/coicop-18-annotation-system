# 📊 دليل تشغيل Dashboard - COICOP

## 🚀 التشغيل السريع

### على Linux/Mac:
```bash
cd dashboard
./run-dashboard.sh
```

### على Windows:
```cmd
cd dashboard
run-dashboard.bat
```

### أو مباشرة:
```bash
python3 dashboard/server.py
```

## 🌐 الوصول

افتح المتصفح على: **http://localhost:7896**

## 📈 الإحصائيات المتوفرة

### البطاقات الرئيسية:
1. **إجمالي البيانات** - عدد العناصر الكلي
2. **مكتمل** - عدد العناصر المكتملة مع نسبة الإنجاز
3. **متبقي** - العناصر قيد الانتظار والمحولة
4. **معدل التوافق** - نسبة توافق المراجع مع الموديل
5. **متوسط الوقت** - متوسط الوقت لكل عنصر (بالثواني)
6. **أيام النشاط** - عدد الأيام والساعات المستخدمة
7. **الوقت المتوقع** - تقدير الوقت للانتهاء
8. **إجمالي القرارات** - العدد الإجمالي للقرارات

### 🏆 المتصدرون:
- ترتيب المحققين حسب الأداء
- إحصائيات لكل محقق:
  - **إجمالي**: عدد القرارات الكلي
  - **اليوم**: عدد القرارات اليوم
  - **توافق**: نسبة التوافق مع الموديل
  - **الوقت**: متوسط الوقت لكل قرار

### 📊 توزيع الإجراءات:
- **قبول**: القرارات الموافقة على اختيار الموديل
- **تصحيح**: القرارات المصححة يدوياً
- **تحويل**: القرارات المحولة للمراجعة المتقدمة

## 🔄 التحديث التلقائي

Dashboard يتحدث تلقائياً كل **30 ثانية** للحصول على آخر البيانات.

## 🐳 التشغيل مع Docker

```bash
docker-compose up dashboard
```

سيعمل Dashboard على المنفذ **7896**.

## 🎨 الميزات

- ✅ خلفية بيضاء نظيفة
- ✅ أيقونات احترافية من Lucide
- ✅ تصميم responsive متجاوب
- ✅ رسوم متحركة سلسة
- ✅ واجهة عربية كاملة
- ✅ تحديث تلقائي للبيانات

## 📝 المتطلبات

- Python 3.x
- Backend يعمل على المنفذ 8000 (أو 8001)

## ⚙️ تغيير المنفذ

لتغيير منفذ Dashboard، عدّل في `server.py`:

```python
PORT = 7896  # غيّر هنا
```

أو عند التشغيل مع Docker، عدّل في `docker-compose.yml`:

```yaml
ports:
  - "YOUR_PORT:7896"
```

## 🔧 API Endpoints المستخدمة

- `GET /api/dashboard/stats`
- `GET /api/dashboard/user-stats`
- `GET /api/dashboard/action-breakdown`

---

**للدعم الفني أو الاستفسارات، يرجى التواصل مع فريق التطوير.**
